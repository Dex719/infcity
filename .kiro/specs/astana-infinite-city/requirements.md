# Requirements: Astana Infinite City

## Document Information

- **Feature Name**: Astana Infinite City (рабочее название проекта — `infcity`)
- **Version**: 1.1 (Approved 2026-09-17; итерация 2 от 2026-09-17 по обратной связи пользователя: FR-14 → Must, добавлены FR-15, FR-16, `bugfix.md`)
- **Status (2026-09-23)**: FR-1…FR-19 реализованы. Итерация 5 (полировка картинки до уровня референса, FR-19) закрыта 2026-09-23: запечённый AO у подножия (стены и ореолы в вершинных цветах), гранёное затенение, тон кадра по оракулу Ламберта (экспозиция ×1,25, светлые тротуары, сочный газон), окна отдельными проёмами, тёмные крыши с цветным парапетом, мощение площадей; волна 2 — облака растворяются у низкой камеры с сохранением теней, серая кровля как у референса, AO стадиона, лотков и опор ЛРТ; волна 3 — открытая чаша стадиона с ярусами и разметкой поля. BUG-10 (2026-09-23): LOD слоя деталей снят по решению пользователя — детали и их тени видны всегда; бюджет возвращён делением статики по солнцу (design D20) без изменения теней; волна 4 — AO контакта у 11 ландмарков, BUG-11 — свод рынка; волна 5 — навесной фасад стеклянных башен и AO Байтерека; визуальный обход — BUG-12, BUG-13 (ТЦ фасадом к камере); волна 6 — полосатые наклонные маркизы и кафе-террасы коммерческих кварталов; волна 7 — белые мягкие облака (свечение материала, 0 треугольников); волна 8 — тёмное стекло окон и витрин (ключ палитры `window`); волна 9 — кровли ТЦ с рядами кондиционеров и фонарями; волна 10 — кондиционеры на кровлях торговых рядов и учебных корпусов; волна 11 — пояса-плиты на фасадах новостроек; волна 12 — AO русла (стенки набережных и полосы на воде); волна 13 — газонные острова с деревьями на деловой площади; волна 14 — газонные полосы с деревьями по краям рынка. FR-20 (2026-09-24): сквер между стартовыми ландмарками — центр старта всегда парк, `GEN.VERSION` 4; он построен бульваром Нуржол (FR-20.4), зелень стартового кадра по 12 seed — 14,9 %. Волна 15 (2026-09-25): пешеходные «зебры» во всю ширину улиц с четырёх сторон перекрёстков, машины встают перед стоп-линией, а не на зебре. Волна 16 — карточка ссылки (Open Graph, кадр 1200 × 630) и значок-Байтерек у вкладки. Волна 17 — детские площадки во дворах (домик с горкой, качели, карусель) и газон во дворе новостройки; зелень стартового кадра — 15,2 %. Кадр — пик 324…338 тыс. треугольников из 400 000 (NFR-1; разброс — от маршрута замера) и 125…127 draw calls из 300 (после волны 17 — 324 тыс. и 126), медианный FPS 101…108 (за вечер 79…140, 2026-09-25 — 45…49 у старого и нового кода — окружение: headless Chrome на Intel UHD, A/B старого и нового кода в пределах разброса); unit 408, e2e 37/37; замер пикселей: газон на свету 173 против 177 у референса, тротуар 214 против 218…233. Итерация 4 (бюджет кадра, улицы, фасады) закрыта 2026-09-19. Evidence по каждому AC — `qa-evidence.md`. Открытые ручные проверки: AC-4.3 (узнаваемость, 5 респондентов), замер FPS на телефоне (NFR-1).
- **Date**: 2026-09-17
- **Author**: Dex719
- **Stakeholders**: автор проекта (владелец продукта и разработчик); зрители демо
- **Reference**: `.kiro/steering/reference-infinitown.md` (разбор Infinitown), `research/infinitown/`

## Introduction

Infinitown (Little Workshop) показал, что «живой» бесконечный low-poly город можно сделать дёшево: конечный стол кварталов, заворачивающийся тором, одно солнце, запечённый AO, машины с простым радаром. Нам нужен **похожий, но свой** опыт с идентичностью **Астаны**: узнаваемые доминанты (Байтерек, Хан Шатыр), надземное **ЛРТ** на опорах, местная застройка и палитра. Не копия 1:1 — своя генерация, свой набор кварталов, свои механики.

Проект — статическое веб-демо (без бэкенда), которое открывается по ссылке, грузится за несколько секунд и «залипает»: город бесконечно тянется под пальцем/мышью, по улицам ездят машины, над проспектом проносится поезд ЛРТ.

### Feature Summary
Бесконечный процедурный low-poly город в стиле Астаны с ЛРТ и ландмарками, панорамируемый мышью/тачем, детерминированный по seed.

### Business Value
- Портфолио/шоукейс WebGL-навыков и любви к городу; вирусный потенциал (шаринг ссылки с seed).
- Основа для дальнейших фич (ночь, зима, экскурсия по ландмаркам).

### Scope

**Включено:**
- Бесконечная сетка чанков (квартал + дороги), детерминированная генерация по seed.
- Ландмарки Астаны: Байтерек и Хан Шатыр (обязательно), Нур Алем, Пирамида (Дворец мира и согласия), Ак Орда (желательно).
- Надземное ЛРТ: эстакада на опорах, станции, движущиеся поезда.
- Автотрафик (легковые, такси, автобусы, спецтранспорт), облака.
- Управление: drag/touch-панорамирование, зум колесом/пинчем, клавиатура.
- Визуальный стиль: low-poly, солнечный день, мягкие тени, туман, виньетка.
- UI-оболочка: заголовок, About, прогресс загрузки, шаринг seed, мобильная раскладка, обработка ошибок.
- Dev-режим со статистикой.

**Исключено (из этой спеки):**
- Реальная карта/GIS-точность Астаны; названия улиц.
- Пешеходы, звук, цикл день/ночь, VR, мультиплеер, бэкенд/аккаунты.
- Копирование ассетов или кода Infinitown (только идеи; clean-room).
- Зима и река Есиль — вынесены в Could (FR-13, FR-14), реализуются после Must/Should.

## User Roles

- **Зритель** — открыл ссылку, смотрит и панорамирует город (десктоп или телефон).
- **Шаринг-пользователь** — хочет показать другу «свой» город (ссылка с seed).
- **Разработчик** — автор; нужны детерминизм, отладочная информация, воспроизводимость.

---

## Requirements

### FR-1: Бесконечный город
**User Story:** Как зритель, я хочу тянуть город в любую сторону бесконечно, чтобы возникало ощущение большого живого мегаполиса.

**Requirements (EARS):**
1. WHEN зритель панорамирует в любом направлении, the system SHALL непрерывно показывать новые чанки без пустот, «дыр» и видимого скачка картинки.
2. WHILE чанк находится за пределами видимого окна, the system SHALL освобождать его ресурсы (не хранить бесконечно растущий мир в памяти).
3. WHEN зритель возвращается к ранее посещённым координатам, the system SHALL показать тот же самый чанк (тот же квартал, поворот, ландмарк, дороги).
4. WHEN камера удаляется от начала координат на любое расстояние, the system SHALL сохранять точность позиционирования (без дрожания геометрии из-за float-ошибок).

**Priority:** Must · **Complexity:** High · **Dependencies:** FR-2, FR-3, FR-8

**Acceptance Criteria:**
- **AC-1.1** Given фиксированный seed, When пользователь панорамирует 200 чанков в одну сторону и возвращается, Then раскладка стартового окна побайтно совпадает с первоначальной (сравнение дескрипторов чанков).
- **AC-1.2** Given любой seed, When выполняется непрерывное панорамирование 60 с, Then ни в одном кадре в видимом окне нет незаполненных слотов (проверка в debug-режиме: `emptySlots === 0`).
- **AC-1.3** Given панорамирование на расстояние 10 000 юнитов от старта, When смотрим на геометрию, Then визуальных артефактов дрожания нет (позиция камеры и слотов остаётся в пределах ±1 чанка от начала координат — сцена сдвигается, а не камера).

---

### FR-2: Детерминированная генерация по seed
**User Story:** Как шаринг-пользователь, я хочу отправить ссылку, по которой друг увидит тот же город, чтобы обсуждать «вон тот квартал у Байтерека».

**Requirements (EARS):**
1. WHEN страница открыта с параметром `?seed=<строка>`, the system SHALL сгенерировать город, полностью определяемый этим seed (на любом устройстве и браузере).
2. WHEN страница открыта без параметра seed, the system SHALL выбрать случайный seed и отразить его в адресной строке (без перезагрузки).
3. WHEN пользователь нажимает «Поделиться» (кнопка внутри окна About, с 2026-09-18), the system SHALL скопировать в буфер обмена ссылку с текущим seed и показать подтверждение.
4. IF seed содержит недопустимые символы или длиннее 64 символов, the system SHALL нормализовать его (обрезать/хешировать) и продолжить работу без ошибки.

**Priority:** Must · **Complexity:** Medium · **Dependencies:** —

**Acceptance Criteria:**
- **AC-2.1** Given seed `astana`, When генерируются дескрипторы чанков в окне 21×21 на двух разных машинах (Windows/macOS, Chrome/Firefox), Then JSON-дампы идентичны.
- **AC-2.2** Given открытие без seed, When страница загрузилась, Then URL содержит `seed=` из 8 символов `[a-z0-9]`, история браузера не растёт (replaceState).
- **AC-2.3** Given нажатие «Поделиться», When буфер обмена доступен, Then в буфере ссылка `https://<host>/?seed=<seed>` и на экране тост «Ссылка скопирована» на 2 с; When буфер недоступен, Then ссылка показана в поле для ручного копирования.

---

### FR-3: Кварталы и дорожная сеть
**User Story:** Как зритель, я хочу видеть разнообразные, но узнаваемо «астанинские» кварталы, чтобы город не выглядел однообразным набором коробок.

**Requirements (EARS):**
1. WHEN генерируется чанк, the system SHALL составить его из квартала (одного из ≥ 8 типов), дорог вдоль двух смежных сторон и перекрёстка в углу так, чтобы дороги соседних чанков стыковались в непрерывную сетку.
2. WHEN выбирается тип квартала, the system SHALL исключить типы, уже использованные в 8 соседних чанках (если исключить невозможно — выбрать наименее частый в окрестности).
3. WHEN квартал помещается в чанк, the system SHALL повернуть его на один из углов 0°/90°/180°/270°, выбранный детерминированно.
4. WHERE тип квартала относится к категории «левый берег», the system SHALL использовать высотную стеклянную застройку; WHERE «жилой», — панельные/кирпичные дома 5–12 этажей; WHERE «торговый», — 2–4-этажные здания с вывесками; WHERE «парк/сквер», — зелень, дорожки, скамейки, фонтан или памятник.
5. WHEN генерируется дорога, the system SHALL размещать разметку, тротуары, фонари и остановки как часть префаба дороги.

**Типы кварталов (минимум 8):** жилой-панельный, жилой-новостройки, деловой-стекло (левый берег), торговый-малоэтажный, парк/сквер, площадь с памятником, университет/кампус, стадион/арена (уникальный: не чаще 1 на 40 чанков).

**Priority:** Must · **Complexity:** High · **Dependencies:** FR-2

**Acceptance Criteria:**
- **AC-3.1** Given 10 000 сгенерированных чанков (любой seed), When проверяем соседства, Then ни один чанк не имеет тот же тип квартала, что и любой из 8 соседей, за исключением случаев, когда все 8 типов заняты соседями (< 0,5 % чанков).
- **AC-3.2** Given любые два соседних чанка, When они собраны в сцене, Then полосы дорог стыкуются без разрыва: расстояние между концами осевых линий ≤ 0,01 юнита.
- **AC-3.3** Given стартовое окно 9×9, When оно отрисовано, Then в нём присутствуют ≥ 6 разных типов кварталов.

---

### FR-4: Ландмарки Астаны
**User Story:** Как зритель, я хочу сразу узнать Астану по Байтереку и Хан Шатыру, чтобы демо читалось как «город Астана», а не абстрактный город.

**Requirements (EARS):**
1. WHEN отрисовано стартовое окно (любой seed), the system SHALL показать Байтерек и Хан Шатыр в пределах стартового вида на фиксированных относительно старта позициях.
2. WHEN генерируется чанк вне стартовой зоны, the system SHALL детерминированно решать, содержит ли он ландмарк, с частотой не выше 1 ландмарка на 30 чанков и минимальным расстоянием между одинаковыми ландмарками ≥ 6 чанков.
3. WHEN чанк содержит ландмарк, the system SHALL занять ландмарком весь квартал чанка (дороги и перекрёсток остаются), с окружением (площадь, аллея, деревья) в стиле ландмарка.
4. WHERE ландмарк — Байтерек, the system SHALL изобразить белую расширяющуюся кверху «крону»-опору с золотым шаром на вершине; высота относительно застройки ≈ в 2–3 раза выше типового жилого дома.
5. WHERE ландмарк — Хан Шатыр, the system SHALL изобразить наклонённый прозрачно-голубой шатёр на мачте с широким основанием.
6. IF ландмарк относится к категории Should (Нур Алем — сфера на подиуме, Пирамида — четырёхгранная пирамида со стеклянной вершиной, Ак Орда — белый дворец с синим куполом и золотым шпилем), the system SHALL добавлять его в пул генерации только когда он реализован (feature-flag в каталоге).

**Priority:** Must (Байтерек, Хан Шатыр) / Should (Нур Алем, Пирамида, Ак Орда) / Could (Астана Опера, Хазрет Султан, Абу-Даби Плаза) · **Complexity:** High · **Dependencies:** FR-3

**Acceptance Criteria:**
- **AC-4.1** Given любой из 100 случайных seed, When открыта страница, Then в стартовом окне присутствуют оба Must-ландмарка, и они видны в кадре камеры на старте (проверка `frustum.intersectsObject`).
- **AC-4.2** Given 10 000 чанков вне стартовой зоны, When считаем ландмарки, Then их доля ≈ N/150 (N — число включённых типов: при 5 типах 1/25…1/40, при 2 — 1/55…1/100), нет пары одинаковых ландмарков ближе 6 чанков (Чебышёв) и нет двух ландмарков в соседних чанках. _(Уточнено 2026-09-17: плотность 1/30 при дистанции ≥ 6 между любыми ландмарками недостижима по упаковке; правило дистанции — per-type.)_
- **AC-4.3** Given скриншот Байтерека и Хан Шатыра при seed `astana`, When его показывают 5 людям, знакомым с Астаной, Then ≥ 4 узнают оба объекта без подсказки (ручной тест на этапе QA).

---

### FR-5: Надземное ЛРТ
**User Story:** Как зритель, я хочу видеть, как по эстакаде над проспектом идёт поезд ЛРТ, чтобы город выглядел современным и «астанинским».

**Requirements (EARS):**
1. WHEN генерируется ряд чанков, помеченный как ЛРТ-коридор (детерминированно: один коридор проходит через стартовое окно, далее коридоры повторяются с фиксированным шагом ≥ 8 чанков), the system SHALL разместить эстакаду (балка на опорах) вдоль дороги этого ряда, непрерывную через все чанки ряда, включая чанки с ландмарками.
2. WHEN чанк ЛРТ-коридора находится на позиции станции (каждый N-й чанк ряда, N = 3), the system SHALL разместить станцию: платформу с навесом и лестницу/лифт на уровень земли.
3. WHILE ЛРТ-коридор виден, the system SHALL показывать поезда (2–3 вагона), движущиеся по эстакаде в обе стороны с постоянной скоростью, с интервалом между поездами ≥ 4 чанка.
4. WHEN поезд достигает станции, the system SHALL плавно затормозить, остановиться на 3–5 с и плавно разогнаться.
5. WHEN поезд пересекает границу чанка, the system SHALL переносить его между чанками без визуального разрыва (как машины).
6. IF два поезда одного направления сближаются ближе 1 чанка, the system SHALL притормозить задний до восстановления интервала.

**Priority:** Must · **Complexity:** High · **Dependencies:** FR-3, FR-6 (общая механика мобильных объектов)

**Acceptance Criteria:**
- **AC-5.1** Given стартовое окно, When оно отрисовано, Then ровно один ЛРТ-коридор проходит через окно и виден в стартовом кадре.
- **AC-5.2** Given любой чанк коридора, When собрана эстакада, Then балка непрерывна: стык между чанками без зазора (≤ 0,01 юнита) и опоры не пересекают дорожное полотно машин.
- **AC-5.3** Given поезд подходит к станции, When замеряем скорость, Then профиль «торможение → стоянка 3–5 с → разгон» воспроизводится, и поезд останавливается в пределах ±2 юнитов от центра платформы.
- **AC-5.4** Given 2 минуты наблюдения за коридором, When считаем поезда, Then в любой момент в окне ≥ 1 поезда и ≤ 4, интервал ≥ 4 чанка.

---

### FR-6: Автомобильный трафик
**User Story:** Как зритель, я хочу видеть машины, которые ездят, тормозят друг перед другом и не проваливаются сквозь здания, чтобы город казался живым.

**Requirements (EARS):**
1. WHEN генерируется чанк, the system SHALL с детерминированной вероятностью (≈ 35 % на полосу на десктопе, ≈ 20 % на мобильных) поставить машину на каждую полосу дороги чанка с направлением по правостороннему движению.
2. WHILE машина движется, the system SHALL сканировать впереди себя (сектор перед машиной и справа, радиус ≈ 1/3 чанка) машины своего и 8 соседних чанков и плавно тормозить при обнаружении.
3. IF машина простояла > 2 с из-за затора, the system SHALL разрешить ей медленное движение (25 % максимальной скорости) для рассасывания взаимных блокировок.
4. WHEN машина пересекает границу чанка, the system SHALL перенести её в соседний чанк с сохранением мировой позиции и направления.
5. WHEN машина находится на перекрёстке, the system SHALL не тормозить перед машинами, стоящими вне перекрёстка на пересекающем направлении.
6. WHEN определяется модель машины, the system SHALL выбирать из пула ≥ 8 вариантов, включая такси, городской автобус, полицию, скорую (уточнено в FR-16: такси — классы Яндекс Go, автобус — Астана).

**Priority:** Must · **Complexity:** Medium · **Dependencies:** FR-3

**Acceptance Criteria:**
- **AC-6.1** Given две машины на одной полосе, задняя быстрее, When задняя приближается на радиус радара, Then она снижает скорость до скорости передней и не пересекает её bbox (нет пересечений в 60 с наблюдения при 100+ машинах).
- **AC-6.2** Given 5 минут симуляции при seed `astana`, When считаем машины со скоростью 0 дольше 10 с, Then их 0 (anti-deadlock срабатывает).
- **AC-6.3** Given машина у границы чанка, When она пересекает границу, Then её мировая позиция в следующем кадре отличается от ожидаемой ≤ 0,05 юнита (нет телепортации).

---

### FR-7: Облака
**User Story:** Как зритель, я хочу видеть плывущие облака с тенями, чтобы сцена дышала.

**Requirements (EARS):**
1. WHEN генерируется чанк, the system SHALL с вероятностью ≈ 30 % добавить облако на высоте над самыми высокими зданиями, дрейфующее в фиксированном «направлении ветра» и отбрасывающее тень.
2. WHILE облако движется, the system SHALL плавно менять его масштаб на ±5 % (дыхание) и переносить между чанками как мобильный объект.

**Priority:** Should · **Complexity:** Low · **Dependencies:** FR-6 (механика мобильных объектов)

**Acceptance Criteria:**
- **AC-7.1** Given стартовое окно, When отрисовано, Then видны 3–12 облаков, их тени падают на землю/крыши.
- **AC-7.2** Given облако у границы окна, When оно выходит за окно, Then появляется с противоположной стороны коридора ветра без скачка (перенос между чанками).

---

### FR-8: Камера и управление
**User Story:** Как зритель, я хочу интуитивно тянуть город мышью или пальцем и приближать колесом/пинчем, чтобы рассматривать детали.

**Requirements (EARS):**
1. WHEN зритель тянет мышью (ЛКМ) или одним пальцем, the system SHALL панорамировать город в направлении жеста с экранной скоростью ≈ 1:1 (объект под курсором остаётся под курсором ±10 %) и плавным доводом после отпускания (≤ 300 мс).
2. WHEN зритель крутит колесо или делает пинч, the system SHALL плавно менять высоту камеры в диапазоне [min, max] с сохранением угла обзора.
3. WHEN зритель нажимает стрелки/WASD, the system SHALL панорамировать с постоянной скоростью, пока клавиша удерживается.
4. WHILE курсор над канвасом, the system SHALL показывать курсор «рука», при удержании — «сжатая рука».
5. WHEN вкладка теряет видимость, the system SHALL ставить симуляцию на паузу и возобновлять при возврате без скачка времени.

**Priority:** Must (1, 2, 4, 5) / Should (3) · **Complexity:** Medium · **Dependencies:** FR-1

**Acceptance Criteria:**
- **AC-8.1** Given drag на 300 px по экрану, When жест завершён, Then точка города, бывшая под курсором в начале, оказывается в пределах 30 px от курсора в конце.
- **AC-8.2** Given 10 «тиков» колеса вниз, When анимация закончилась, Then камера на минимальной высоте (`CAMERA.HEIGHT_MIN`, с итерации 2 — 60: точка обзора выше любой застройки, см. `bugfix.md` BUG-1), следующие тики не меняют её; аналогично вверх до максимума.
- **AC-8.3** Given скрытие вкладки на 30 с, When вкладка возвращена, Then машины не «прыгают» (dt ограничен 50 мс).

---

### FR-9: Визуальный стиль
**User Story:** Как зритель, я хочу цельную «игрушечную» картинку в цветах Астаны, чтобы демо выглядело законченно и приятно.

**Requirements (EARS):**
1. WHEN сцена отрисована, the system SHALL использовать один направленный солнечный свет с мягкими тенями и рассеянный свет неба (без «плоских» неосвещённых теневых сторон).
2. WHEN объект удаляется от камеры, the system SHALL растворять его в тумане цвета неба так, чтобы край окна чанков не был виден.
3. WHEN сцена отрисована, the system SHALL применять лёгкую виньетку по краям кадра.
4. WHERE палитра материалов, the system SHALL использовать набор цветов «Астана»: светлый камень/бежевый, белый, голубое и бирюзовое стекло, золото (Байтерек, купола), синий флага как акцент, зелёный газонов, серый асфальт с белой разметкой.
5. WHEN отрисованы здания, the system SHALL показывать flat-shading/фасетный low-poly без фотореалистичных текстур.

**Priority:** Must · **Complexity:** Medium · **Dependencies:** FR-3, FR-4

**Acceptance Criteria:**
- **AC-9.1** Given скриншот стартового окна при seed `astana`, When сравниваем с эталоном (visual regression), Then различие ≤ 1 % пикселей.
- **AC-9.2** Given камера на максимальной высоте, When смотрим на дальний край, Then край окна чанков не читается (яркость дальнего края отличается от неба ≤ 5 %).
- **AC-9.3** Given любой материал в каталоге, When проверяем цвет, Then он входит в палитру `palette.json` (не более 24 цветов + прозрачное стекло).

---

### FR-10: UI-оболочка
**User Story:** Как зритель, я хочу понять, что это за демо и кто автор, поделиться им, и не путаться на телефоне.

**Requirements (EARS):**
1. WHEN загрузка завершена и демо запущено, the system SHALL показать анимированный заголовок (название), который через несколько секунд исчезает.
2. WHEN зритель нажимает клавишу `?`, the system SHALL открыть окно About (описание, «сделано с», credits ассетов, ссылка на автора, кнопка «Поделиться»), поставить симуляцию на паузу и размыть/затемнить сцену. *(Изменено 2026-09-18 по запросу пользователя: экранных кнопок в углу нет.)*
3. WHEN зритель закрывает About (крестик, клик вне окна, Esc), the system SHALL снять размытие и возобновить симуляцию.
4. WHERE ширина экрана < 700 px, the system SHALL показывать About на весь экран с прокруткой и увеличенными элементами управления.
5. WHEN UI отрисован, the system SHALL масштабировать типографику от размера окна (rem/vw), не ломая раскладку от 320 px до 4K.

**Priority:** Must · **Complexity:** Low · **Dependencies:** FR-11

**Acceptance Criteria:**
- **AC-10.1** Given старт демо, When прошло 500 мс после `started`, Then заголовок появляется анимацией и через 7 с плавно исчезает.
- **AC-10.2** Given открытое About, When замеряем `deltaTime` симуляции, Then он 0 (пауза) и канвас имеет фильтр размытия; после закрытия — фильтр снят, симуляция идёт.
- **AC-10.3** Given viewport 320×568 и 3840×2160, When открыто About, Then текст не выходит за экран и кнопки кликабельны (Playwright).

---

### FR-11: Загрузка и ошибки
**User Story:** Как зритель, я хочу видеть честный прогресс загрузки и понятное сообщение, если что-то не работает.

**Requirements (EARS):**
1. WHILE грузятся ассеты, the system SHALL показывать прогресс-бар, рассчитанный по фактическому числу/объёму ресурсов (не по константе).
2. IF загрузка ресурса завершилась ошибкой, the system SHALL повторить попытку до 2 раз, затем показать сообщение об ошибке с кнопкой «Повторить».
3. IF браузер не поддерживает WebGL 2, the system SHALL показать статичную заставку с текстом-пояснением и ссылкой на поддерживаемые браузеры.
4. WHEN контекст WebGL потерян и восстановлен, the system SHALL восстановить сцену без перезагрузки страницы; IF восстановление не удалось за 5 с, the system SHALL предложить перезагрузку.
5. WHEN все ресурсы загружены, the system SHALL начать рендер не позднее чем через 200 мс.

**Priority:** Must · **Complexity:** Medium · **Dependencies:** —

**Acceptance Criteria:**
- **AC-11.1** Given троттлинг сети «Fast 3G», When грузится страница, Then прогресс-бар монотонно растёт от 0 до 100 % и достигает 100 % ровно в момент завершения последней загрузки.
- **AC-11.2** Given ассет отвечает 500, When загрузка, Then в консоли 3 попытки, затем оверлей с «Повторить»; клик по «Повторить» перезапускает загрузку.
- **AC-11.3** Given `WebGL2RenderingContext` отсутствует (мок), When открыта страница, Then показана заставка, ошибок в консоли нет.

---

### FR-12: Dev-режим
**User Story:** Как разработчик, я хочу видеть FPS, draw calls, seed и координаты чанка, чтобы отлаживать генерацию и производительность.

**Requirements (EARS):**
1. WHEN страница открыта с `?debug=1`, the system SHALL показать оверлей: FPS, draw calls, число мешей/инстансов, seed, `gridCoords`, число машин/поездов/облаков, пустые слоты.
2. WHEN включён debug, the system SHALL рисовать границы чанков и подписи `(gx, gy)`.
3. WHEN включён debug, the system SHALL экспонировать `window.__app` (генератор, окно чанков, конфиг) для e2e-тестов.

**Priority:** Should · **Complexity:** Low · **Dependencies:** FR-1

**Acceptance Criteria:**
- **AC-12.1** Given `?debug=1&seed=astana`, When страница загружена, Then оверлей показывает `seed=astana`, `gridCoords=(0,0)`, `emptySlots=0`.
- **AC-12.2** Given `?debug=1`, When панорамируем на 1 чанк, Then `gridCoords` меняется на ±1 по соответствующей оси.

---

### FR-13: Зимний режим (Could)
1. WHEN страница открыта с `?season=winter`, the system SHALL заменить палитру на зимнюю (снег на крышах, газонах и обочинах, серо-голубое небо, тёплый низкий свет) без изменения геометрии генерации.

**Priority:** Could · **AC-13.1** Given `season=winter`, When скриншот, Then ≥ 40 % площади земли — белый снег, seed-раскладка идентична летней.

### FR-14: Река Есиль с мостами (Must с итерации 2)
1. WHEN генерируется ряд чанков, помеченный как русло (детерминированно, период 12, ряды не совпадают с коридорами ЛРТ), the system SHALL заменить квартал водой с набережной, а дороги N–S — мостами, сохраняя стыковку дорожной сети; ЛРТ проходит над рекой по эстакаде.

**Priority:** Must (повышено 2026-09-17 по запросу пользователя: «добавь реку с Астаны, мостик») · **AC-14.1** Given чанк русла, When собран, Then дороги соседних чанков соединены мостом без разрыва, машины проезжают.

---

### FR-15: Узнаваемая Астана — застройка и детали (итерация 2, запрос пользователя 2026-09-17)
**User Story:** Как зритель, знакомый с Астаной, я хочу узнавать город по знакомым зданиям, флагу, реке и деталям, а не только по двум ландмаркам.

**Requirements (EARS):**
1. WHEN выбирается тип квартала, the system SHALL не использовать тип «завод/промзона» (`industrial`); вместо него — «торговый центр» (`mall`): крупный ТЦ с вывеской, стеклянным входом и парковкой.
2. WHEN генерируются ландмарки, the system SHALL размещать по правилу редкости (FR-4.2) дополнительно: Абу-Даби Плаза, Астана Опера, Хазрет Султан, Mega Silk Way (Must); «Северное сияние», Транспортная башня («Зажигалка»), КазМунайГаз с аркой (Should).
3. WHEN собирается ландмарк, the system SHALL добавлять детализацию: Байтерек — орнамент площади, пояса ствола, вход; Хан Шатыр — рёбра шатра, арка входа, флагштоки; Нур Алем — панели сферы, павильоны Expo и ≥ 3 фигуры роботов у входа; Пирамида — подиум с лестницами, вершина; Ак Орда — колоннада, ворота, флаги.
4. WHEN собирается флаг, the system SHALL использовать флаг Казахстана: голубое полотно, золотое солнце с лучами и орёл под ним (силуэт), золотой орнамент у древка — низкополигонально, но узнаваемо.
5. WHEN собираются крыши жилых/деловых/торговых зданий, the system SHALL детерминированно добавлять детали не менее чем на 40 % крыш: антенны, блоки кондиционеров, баки, спутниковые тарелки, вентиляционные короба, вывески на ТЦ.
6. WHEN генерируется город, the system SHALL отражать местность Астаны: к северу от ближайшего русла («правый берег») преобладает панельная застройка старого города, к югу («левый берег») — стеклянные высотки, ТЦ и ландмарки.

**Priority:** Must · **Complexity:** High · **Dependencies:** FR-3, FR-4, FR-14

**Acceptance Criteria:**
- **AC-15.1** Given seed `astana`, When дамп окна 21×21, Then ни одного чанка `industrial`; `mall` встречается ≥ 1 раза на 40 чанков.
- **AC-15.2** Given каждый новый ландмарк, When `__app.centerOn` и скриншот, Then visual-эталон зафиксирован; узнаваемость — ручной тест (расширение AC-4.3).
- **AC-15.3** Given чанк с флагштоком, When собран, Then флаг содержит голубое полотно, золотое солнце и орнамент (≥ 3 элемента геометрии разных цветов).
- **AC-15.4** Given 200 зданий жилых/деловых/торговых типов, When собраны, Then ≥ 40 % имеют хотя бы одну деталь на крыше (счётчик в `Buildings`).
- **AC-15.5** Given полосы между руслами (левый и правый берег, ряды −5…5 и 7…17 при seed `astana`), When считаем регулярные типы по 61 столбцу, Then на левом берегу доля «стекло + ТЦ + новостройки» ≥ 50 %, на правом доля «панельки + новостройки» ≥ 33 % и стекла < 10 % (правило соседства ограничивает любой один тип ≤ 25 %, поэтому «панельки» не могут доминировать сильнее).

---

### FR-16: Транспорт Астаны — автобусы и такси (итерация 2)
**User Story:** Как зритель, я хочу видеть на улицах узнаваемые астанинские автобусы и машины Яндекс Go вместо абстрактного «жёлтого такси».

**Requirements (EARS):**
1. WHEN выбирается модель автобуса, the system SHALL использовать простую модель городского автобуса Астаны: светлый кузов с сине-зелёно-жёлтыми элементами брендинга (новые автобусы CTS 2024), большие окна, табло маршрута.
2. WHEN выбирается модель такси, the system SHALL использовать три класса Яндекс Go: «Эконом/Комфорт» — белая машина с жёлтой полосой на дверях и жёлтым знаком на крыше; «Бизнес» — чёрный седан; «Ultima Premier» — чёрный премиальный седан без брендинга.
3. WHEN определяется модель машины, the system SHALL выбирать из пула ≥ 10 моделей (пул расширен, версия генератора повышена до 2).

**Priority:** Must · **Complexity:** Low · **Dependencies:** FR-6

**Acceptance Criteria:**
- **AC-16.1** Given пул моделей, Then в нём автобус Астаны и 3 класса Яндекс Go; распределение по чанкам детерминировано seed (снапшот).
- **AC-16.2** Given улица с автобусом и такси на минимальной высоте камеры, When скриншот, Then visual-эталон зафиксирован.

---

### FR-17: Детализация — достопримечательности, деревья, облака (итерация 3, запрос пользователя 2026-09-18)
**User Story:** Как зритель, я хочу, чтобы при приближении камеры ландмарки, деревья и облака не выглядели «пустыми» болванками: у деревьев — объёмные кроны, у облаков — форма и тень, у достопримечательностей — окна, навесы, изгороди, светильники и фирменные эмблемы.

**Requirements (EARS):**
1. WHEN собирается дерево, the system SHALL строить крону из нескольких объёмов двух оттенков зелёного (лиственное — ≥ 3 «облака» кроны, хвойное — 3 яруса), ствол с утолщением у основания и приствольный круг; and SHALL поддерживать третий вид — тополь (высокая узкая крона, характерная для Астаны). Вид дерева определяется детерминированно seed без дополнительных вызовов генератора случайных чисел.
2. WHEN собирается облако, the system SHALL использовать ≥ 3 силуэта (кучевое, крупное кучевое, вытянутое слоистое) из ≥ 7 объёмов каждый, с более тёмной подложкой снизу («тень» облака) и плоским основанием; число моделей — `CLOUD.MODELS` = 3, версия генератора повышается до 3.
3. WHEN собирается любой из 12 ландмарков, the system SHALL добавлять не менее 3 новых узнаваемых деталей по таблице design «Итерация 3 → C8»: ряды окон/панелей, навесы входов, живые изгороди и клумбы, светильники-прожекторы, столбики, эмблемы/шпили; общие детали — через помощники `Props` (`hedge`, `flowerBed`, `canopy`, `bollards`, `spotlight`), специфичные — в файле ландмарка.
4. WHEN детали добавлены, the system SHALL укладываться в NFR-1 (≤ 400 000 треугольников и ≤ 300 draw calls в окне 9×9); любой регулярный квартал — не более 7 000 вершин, любой ландмарк — не более 9 000 вершин (уточнено 2026-09-18 по измерениям: относительные пороги заменены абсолютными — у Пирамиды базовое число вершин было в 5 раз меньше панельного квартала, а парк уже в первой волне вырос в 1,58 раза).

**Волна 2 (запрос пользователя 2026-09-18 «ещё больше деталей»):**
5. WHEN в парке, сквере, дворе или кампусе выбирается дерево, the system SHALL с долей ≈ 10 % (тот же бросок rng) строить «цветущее» дерево (розовая крона с белыми цветами — яблони и сирень Астаны); and WHEN собирается регулярный квартал, the system SHALL добавлять ≥ 2 элемента благоустройства по типу квартала (кусты `Props.bush`, изгороди, клумбы, столбики, ограждения парковок) по таблице design C7 (волна 2).
6. WHEN собирается облако, the system SHALL использовать три тона (верх, средний слой, подложка) и ≥ 2 малых «клочка» по краям силуэта — не менее 10 объёмов на модель.
7. WHEN собирается ландмарк, the system SHALL добавлять ещё ≥ 3 детали (мачты освещения, скамейки, киоски, ряды окон минаретов, рисунок мощения, флаги, вентиляция на крышах) по таблице design C8 (волна 2).

**Волна 3 (запрос пользователя 2026-09-18 «шар Байтерека — треугольниками, реализма»):**
8. WHEN собирается Байтерек, the system SHALL строить шар не гладкой сферой, а гранёным: ≥ 300 треугольных панелей с плоскими нормалями (каждая грань читается отдельно), двумя чередующимися оттенками золота и металлическими ободами по экватору и меридиану — как панельная конструкция настоящего шара.

**Priority:** Must · **Complexity:** Medium · **Dependencies:** FR-3.4, FR-4, FR-7, FR-15

**Acceptance Criteria:**
- **AC-17.1** Given `Props.tree` трёх видов, When каждое собрано в пустой батч, Then лиственное содержит ≥ 4 элемента геометрии (ствол + ≥ 3 объёма кроны) и ≥ 2 оттенка зелёного, хвойное — ≥ 3 яруса, тополь — высота кроны ≥ 2,5 её ширины (unit по вершинам/цветам батча).
- **AC-17.2** Given `CLOUD.MODELS` = 3, When каждая модель собрана, Then ≥ 7 объёмов и ≥ 2 цвета (белый и тень); дескрипторы содержат `cloud.model` ∈ {0, 1, 2}; `GEN.VERSION` = 3, снапшот генератора обновлён.
- **AC-17.3** Given каждый из 12 ландмарков, When собран в пустой контекст, Then число вершин батча ≥ порога итерации 2 × 1,35 (две волны) и ≤ 9 000 (таблица порогов в unit-тесте), и visual-эталон `landmark-*.png` обновлён; узнаваемость — ручной тест (расширение AC-4.3).
- **AC-17.4** Given окно 9×9 при seed `astana`, When `e2e/perf.spec.ts`, Then треугольники ≤ 400 000 и draw calls ≤ 300 (без изменения порогов); Given окно 21×21, Then максимум вершин любого регулярного типа ≤ 7 000.
- **AC-17.5** Given `Props.tree` вида 3 и `Props.bush`, When собраны, Then крона цветущего дерева содержит ≥ 2 незелёных оттенка (розовый, белый) при ≥ 3 объёмах, куст — один объём; в окне 21×21 seed `astana` цветущие деревья встречаются.
- **AC-17.6** Given каждая модель облака, Then ≥ 10 объёмов и ≥ 3 цвета.
- **AC-17.7** Given окно 21×21 seed `astana`, When собраны кварталы, Then максимум вершин каждого регулярного типа (кроме `river`) ≥ ×1,05 значения итерации 2 (детали благоустройства добавлены) и ≤ 7 000.
- **AC-17.8** Given гранёный шар (`Templates.icoFlat` через `GeometryBatch.placeFacets`), When собран, Then ≥ 300 граней, нормали трёх вершин каждой грани совпадают (плоская грань), в батче ровно 2 цвета; эталон `landmark-baiterek.png` обновлён, шар Байтерека в бюджете 9 000 вершин.

---

### FR-18: Детализация — улицы, фасады и бюджет кадра (итерация 4, запрос пользователя 2026-09-19)
**User Story:** Как зритель, я хочу, чтобы улицы и фасады читались так же подробно, как ландмарки: разметка, знаки и светофоры на дорогах, урны и велопарковки на тротуарах, балконы, подъезды и витрины на домах, — и чтобы город при этом не терял плавность.

**Контекст.** После трёх волн итерации 3 бюджет кадра выбран на 94 % (пик 377 596 треугольников из 400 000, замер `test-results/perf.json` 2026-09-17), при этом draw calls — 104 из 300. Дальнейшая детализация «в лоб» упирается в NFR-1 после двух-трёх изменений, поэтому итерация начинается с освобождения бюджета, а не с новых деталей.

**Requirements (EARS):**
1. WHEN собирается геометрия квартала, здания или дороги, the system SHALL не строить элементы, обращённые к мировым сторонам −X и −Z: направление взгляда задано `CAMERA.OFFSET` (+X, +Z) и не вращается ни одним элементом управления, поэтому грани с нормалями −X/−Z не попадают в кадр ни при какой высоте и позиции камеры. IF квартал повёрнут (`descriptor.rotation` ∈ {0,1,2,3}), THEN скрытые **локальные** стороны вычисляются из поворота, а не берутся фиксированными.
2. WHEN собирается чанк с дорогами, the system SHALL добавлять разметку сверх осевой: стоп-линии перед зебрами, стрелки направления движения на полосах у перекрёстка и обозначение края проезжей части у тротуара.
3. WHEN перекрёсток имеет вариант `lights`, the system SHALL дополнять его дорожными знаками на стойках (щит на столбике) и пешеходными секциями светофора; WHERE вариант `plaza` — знаками и указателем.
4. WHEN собирается тротуар вдоль квартала, the system SHALL расставлять уличную мебель: урны у остановок и переходов, велопарковку в кварталах `business-glass`, `campus`, `mall`, скамейки вдоль тротуара варианта `b`.
5. WHEN квартал имеет парковку (`commercial`, `mall`, `business-glass`, `stadium`), the system SHALL размечать машино-места линиями и ставить на часть из них `Props.parkedCar`.
6. WHEN собирается жилой или торговый дом, the system SHALL добавлять фасадные детали: балконы (панельный дом), входную группу с крыльцом и ступенями, цоколь и карниз, витрины первого этажа с вывеской (торговый ряд).
7. WHEN добавляется любая деталь итерации 4, the system SHALL не изменять число и порядок обращений к `rng` существующих префабов: дескрипторы генератора, снапшоты `tests/world/__snapshots__/*` и `GEN.VERSION` остаются прежними — раскладка города не меняется, меняется только её наполнение.
8. WHEN детали итерации 4 добавлены, the system SHALL укладываться в NFR-1 (≤ 400 000 треугольников, ≤ 300 draw calls) и в потолки FR-17.4 (регулярный квартал ≤ 7 000 вершин, ландмарк ≤ 9 000 вершин).
9. WHEN чанк находится дальше `DETAIL.HIDE_DISTANCE` от камеры, the system SHALL не рисовать его мелкие детали (деревья, кусты, клумбы, изгороди, фонари, скамейки, столбики, дорожную разметку, детали крыш), сохраняя крупные формы: здания, дорожное полотно, тротуары, эстакаду ЛРТ и основные объёмы ландмарков. Переключение SHALL выполняться сменой видимости готового меша — без пересборки геометрии чанка, без обращения к кэшу и без изменения дескриптора.
10. WHEN чанк пересекает границу детализации, the system SHALL применять гистерезис (`DETAIL.SHOW_DISTANCE` < `DETAIL.HIDE_DISTANCE`) и держать обе границы в зоне тумана (`RENDER.FOG.near` = 225 ≤ граница ≤ `RENDER.FOG.far` = 325), чтобы появление и исчезновение деталей происходило в дымке и не читалось как мигание.
11. WHEN окно телепортируется на другой чанк (`centerOn` из debug-API и e2e), the system SHALL пересчитывать видимость деталей без гистерезиса: кадр после телепорта SHALL зависеть только от положения камеры, но не от того, где чанк находился раньше.
12. WHEN видимость деталей пересчитана, the system SHALL показывать детали не более чем у `DETAIL.MAX_DETAIL_SLOTS` слотов окна на любой высоте камеры, причём слоты с равным расстоянием до камеры SHALL получать одинаковое решение (группы не разрываются).
13. WHEN чанк дальше `DETAIL.SHADOW_DISTANCE` от камеры, the system SHALL не включать его слой деталей в теневой проход, продолжая рисовать сами детали: тень мелочи читается только вблизи, а в карту теней попадает заметно больше чанков, чем различает глаз.

> **Отменено 2026-09-23 (BUG-10, решение пользователя «убери оптимизацию»):** пункты 9–13 и AC-18.9…AC-18.13 — LOD слоя деталей и тень деталей только вблизи давали видимое появление объектов и теней при панорамировании и зуме. Детали снова часть статики чанка, видны и отбрасывают тень всегда; бюджет кадра — NFR-1 (≤ 400 000 треугольников).

**Priority:** Must · **Complexity:** Medium · **Dependencies:** FR-3.1, FR-3.4, FR-3.5, FR-15, FR-17, NFR-1, NFR-3

**Acceptance Criteria:**
- **AC-18.1** Given отсечение невидимых сторон (FR-18.1), When прогоняется `e2e/visual.spec.ts` на **прежних** эталонах без `--update-snapshots`, Then все 12 снимков ландмарков и 6 обзорных кадров проходят (порог `maxDiffPixelRatio` 0.01) — удалённая геометрия не была видна; and максимум вершин `residential-panel` в окне 21×21 падает не менее чем на 15 % (замер 2026-09-19: 5 802 → 4 852, −16 %). Пик треугольников при этом снижается слабо (377 596 → 367 108, −2,8 %): оконная полоса стоит 24 вершины при 12 треугольниках, поэтому отсечение освобождает память и время сборки, а не бюджет треугольников — его освобождает LOD (FR-18.9).
- **AC-18.2** Given `descriptor.rotation` ∈ {0,1,2,3}, When вычисляются скрытые локальные стороны квартала, Then для каждого поворота ровно две стороны скрыты и это те, что отображаются в мировые −X и −Z (unit-тест таблицы поворотов).
- **AC-18.3** Given чанк с `roads.corner = 'lights'`, When собран, Then в батче присутствуют стоп-линии перед каждой зеброй, ≥ 2 стрелки направления, ≥ 2 знака на стойке; все элементы разметки лежат на `y = MARK_Y` (0.03) и не конфликтуют по глубине с полотном (unit по числу и высоте элементов).
- **AC-18.4** Given окно 21×21 seed `astana`, When собраны все чанки, Then встречаются урна, велопарковка и скамейка тротуара; максимум вершин любого регулярного типа ≤ 7 000.
- **AC-18.5** Given квартал `commercial` или `mall`, When собран, Then разметка машино-мест содержит ≥ 4 линии и ≥ 1 `parkedCar`.
- **AC-18.6** Given панельный дом и торговый ряд, When собраны, Then панельный дом содержит ≥ 4 балкона и входную группу со ступенями, торговый ряд — витрину и вывеску на фасаде +Z (unit по числу частей батча).
- **AC-18.7** Given любая задача итерации 4, When прогоняется `npm run test`, Then снапшоты `tests/world/__snapshots__/*` не изменились и `GEN.VERSION` = 3 (детерминизм раскладки, FR-18.7).
- **AC-18.8** Given окно 9×9 seed `astana`, When `e2e/perf.spec.ts`, Then треугольники ≤ 400 000 и draw calls ≤ 300 (пороги NFR-1 не поднимаются).
- **AC-18.9** Given LOD-слой деталей (FR-18.9) и окно 9×9 seed `astana`, When `e2e/perf.spec.ts` панорамирует на стартовой высоте, Then пик треугольников ≤ 330 000 при draw calls ≤ 300; Given чанк в центре окна, Then его детали видны на любой высоте камеры. Замер 2026-09-19: 324 542 треугольника (было 367 108, −11,6 %) и 155 draw calls (было 104). Первоначальная цель «≤ 300 000» не достигнута и заменена измеренной: дальние чанки и без LOD отсекались отсечением по пирамиде видимости, поэтому выигрыш дают только чанки в кадре за границей тумана. Следующий рычаг — сдвинуть `DETAIL.SHOW_DISTANCE`/`HIDE_DISTANCE` ближе, но это выведет переключение из тумана (нарушит FR-18.10) либо потребует приблизить `RENDER.FOG.near`, то есть изменить вид; решение за пользователем.
- **AC-18.10** Given зум с высоты 140 на 60 и обратно, When выполняется смена высоты, Then счётчик `builds` окна чанков не растёт (пересборки нет) — unit-тест на переключение видимости; and unit-тест гистерезиса: чанк за `HIDE_DISTANCE` гасит детали, ближе `SHOW_DISTANCE` — зажигает, между границами состояние не меняется.
- **AC-18.11** Given окно с деталями, выставленными полностью включёнными и полностью выключенными, When видимость пересчитана со `snap` (телепорт), Then обе раскладки совпадают — состояние не зависит от истории (FR-18.11).
- **AC-18.12** Given высоты камеры 60, 90 и 140, When видимость пересчитана, Then число чанков с видимыми деталями > 0 и ≤ `DETAIL.MAX_DETAIL_SLOTS` (FR-18.12).
- **AC-18.13** Given окно 9×9, When видимость пересчитана, Then существуют чанки с видимыми деталями и без тени, а все чанки с тенью имеют видимые детали; and пик треугольников падает не менее чем на 15 % относительно варианта, где весь слой деталей отбрасывает тень. Замер 2026-09-19: 326 350 → 259 946 (−20,3 %), draw calls 147 → 123, тени ближней зоны на кадре сохранены (визуальное сравнение).

---

### FR-19: Полировка картинки до уровня референса (итерация 5, запрос пользователя 2026-09-23 «Отпалируй проект до уровня Infinitown»)
**User Story:** Как зритель, я хочу, чтобы город выглядел так же «дорого» и цельно, как Infinitown: мягкая тень у подножия зданий, гранёные кроны и облака, светлые тротуары и сочные газоны, крыши с парапетами, отдельные окна на фасадах — при той же плавности.

**Контекст.** Сравнение кадров 2026-09-23 (оригинал `demos.littleworkshop.fr/demos/infinitown/` и `?seed=astana`, оба 1280×720 на стартовой высоте, пиксели сняты скриптом) показало, что разрыв не в числе деталей, а в «запечённости» и тоне картинки:

| Признак | Референс | infcity (итерация 4) |
|---|---|---|
| Контакт здания с землёй | мягкое затемнение (AO запечён в карты `uv2`) | резкая линия, затемнения нет |
| Кроны, облака, купола | гранёные low-poly, светлые и тёмные грани читаются | гладкие шары |
| Тротуар на свету, яркость sRGB | 218…233, холодный белый | ≈ 180, бежевый |
| Асфальт на свету | 103 | 79 |
| Газон на свету | 177 (138, 193, 67) | ≈ 150 расчётно, темнее и глуше |
| Тень / свет на одном материале | 0,56, тень синее света | 0,50…0,58, тень синее — **уже на уровне** |
| Крыши | тёмная кровля + цветной парапет | белая плита — кадр сверху «выбелен» |
| Окна | отдельные проёмы | сплошная лента на этаж |
| Большие площади | почти нет, кварталы заполнены | однотонные поля `stone-light` |

**Requirements (EARS):**
1. WHEN собирается корпус или цоколь здания, стоящий на земле квартала, the system SHALL запекать в вершинные цвета затемнение у основания стен (AO контакта): множитель `AO.WALL_MIN` у основания плавно растёт до 1 на высоте `AO.WALL_HEIGHT` над ним — независимо от полной высоты стены (дополнительный пояс вершин).
2. WHEN здание стоит на земле квартала, the system SHALL строить вокруг его цоколя плоский ореол цвета земли под ним: у стены — `AO.GROUND_MIN` от цвета земли, к внешнему краю шириной `AO.GROUND_WIDTH` — ровно цвет земли (без видимого шва). Ореолы соседних зданий SHALL не перекрываться: на сближенных сторонах ширина урезается до половины зазора.
3. WHEN рисуется непрозрачная геометрия города (застройка, деревья, облака, машины, поезда), the system SHALL применять плоское затенение (нормаль на грань): кроны, облака, купола и цилиндры читаются как гранёный low-poly; число треугольников при этом не растёт.
4. WHEN сцена освещена, the system SHALL держать тон картинки на уровне референса: на свету тротуар ≥ 210, асфальт 90…115, газон ≥ 150 (яркость sRGB); отношение яркостей тень/свет на одном материале — 0,50…0,62, тень холоднее света (отношение синего к красному в тени выше, чем на свету).
5. WHEN строится плоская крыша жилого, торгового, учебного или делового здания, the system SHALL делать кровлю тёмной (`roof-dark`) и обносить её парапетом высотой `ROOF.PARAPET_HEIGHT` с цветным верхом — акцентом здания, выбранным детерминированно из цвета стен (без `rng`).
6. WHEN строятся окна жилых, торговых и учебных зданий, the system SHALL рисовать отдельные проёмы с шагом `FACADE.WINDOW_STEP` вместо сплошной ленты на этаж — только на видимых сторонах (D13) и не дороже ленты по треугольникам.
7. WHEN собирается квартал с большим мощёным полем (`square`, `business-glass`), the system SHALL разбивать поле швами мощения и зелёными вставками, не оставляя однотонного свободного прямоугольника больше `PAVING.MAX_EMPTY` на сторону.
8. WHEN добавляется любая деталь итерации 5, the system SHALL не менять число и порядок обращений к `rng` существующих префабов (как FR-18.7): снапшоты генератора и `GEN.VERSION` = 3 остаются прежними.
9. WHEN изменения итерации 5 внесены, the system SHALL укладываться в бюджеты: пик ≤ 330 000 треугольников и ≤ 300 draw calls (AC-18.9), потолки вершин 7 000 на регулярный квартал и 9 000 на ландмарк (FR-17.4).

**Priority:** Must (1–5, 8, 9), Should (6, 7) · **Complexity:** Medium · **Dependencies:** FR-9, FR-17, FR-18, NFR-1, NFR-3

**Acceptance Criteria:**
- **AC-19.1** Given `GeometryBatch.boxAo` высотой 30 при `AO.WALL_HEIGHT` 3, When собран, Then у вершин боковых граней на уровне основания множитель цвета равен `AO.WALL_MIN`, на высоте ≥ `AO.WALL_HEIGHT` — 1 (± 1e-6); верх и низ не затемнены; бокс замкнут — 32 вершины и 20 треугольников (unit). Замкнутость обязательна: теневой проход three рисует обратные грани, и без дна у основания стены на теневой стороне появится светлая щель.
- **AC-19.2** Given квартал `residential-panel`, When собран, Then вокруг каждого дома есть ореол: вершины внутреннего края темнее цвета земли ровно в `AO.GROUND_MIN` раз, внешнего — равны ему; ореолы двух домов не пересекаются (unit по прямоугольникам).
- **AC-19.3** Given `Materials`, When создан, Then у `opaque` включено `flatShading`, у `glass` — нет; and пик треугольников `perf.spec.ts` отличается от прогона без флага не более чем на 1 % (флаг не добавляет геометрию).
- **AC-19.4** Given `RENDER.SUN`, `RENDER.HEMISPHERE_INTENSITY` и палитра, When считается модель Ламберта three (`RE_Direct_Lambert` + `RE_IndirectDiffuse_Lambert`, делитель π, вывод в sRGB), Then на горизонтальной поверхности на свету `sidewalk` ≥ 210, `asphalt` ∈ [90, 115], `grass` ≥ 150; отношение тень/свет для `sidewalk` ∈ [0,50; 0,62]; B/R в тени > B/R на свету (unit по формуле); and замер пикселей стартового кадра в qa-evidence подтверждает расчёт с точностью ± 12.
- **AC-19.5** Given дома пяти типов с плоской крышей, When собраны, Then у каждого 4 стенки парапета высотой `ROOF.PARAPET_HEIGHT`, цвет верха отличается от цвета кровли, кровля — `roof-dark`; потолки 7 000 / 9 000 соблюдены (unit).
- **AC-19.6** Given панельный дом 16 × 10 в 9 этажей, When собран, Then окна — отдельные четырёхугольники (≥ 4 на этаж на длинной видимой стороне), все лежат на видимых сторонах, треугольников окон не больше, чем у лент итерации 4 на том же доме (unit).
- **AC-19.7** Given кварталы `square` и `business-glass`, When собраны, Then в батче есть швы мощения и ни один свободный однотонный прямоугольник покрытия не превышает `PAVING.MAX_EMPTY` на сторону (unit по раскладке).
- **AC-19.8** Given любая задача итерации 5, When `npm run test`, Then снапшоты `tests/world/__snapshots__/*` не изменились, `GEN.VERSION` = 3.
- **AC-19.9** Given окно 9×9 seed `astana`, When `e2e/perf.spec.ts`, Then пик ≤ 330 000 треугольников и ≤ 300 draw calls; `tests/scene/landmarks.test.ts` — потолки 7 000 / 9 000 без изменения порогов.
- **AC-19.10** Given кадры `?seed=astana` на стартовой высоте и после 5 щелчков колеса, When сравниваются с кадрами референса того же размера, Then в qa-evidence записаны замеры пикселей и чек-лист признаков из таблицы контекста (ручная приёмка; снимки референса в репозиторий не кладутся — только замеры).

**Волна 2 (2026-09-23, по расхождениям, записанным в qa-evidence итерации 5):**

10. WHEN камера опускается, the system SHALL плавно растворять облака в полосе высот `CLOUD.FADE.HIDE`…`CLOUD.FADE.SHOW` вместо мгновенного скрытия, так чтобы видимое облако не было крупнее ≈ 2,5 масштаба города (отношение `h / (h − CLOUD.ALTITUDE)`), и SHALL сохранять тени облаков на земле при любой высоте камеры.
11. WHEN сцена освещена, the system SHALL рисовать кровлю плоских крыш светлее — ближе к серым крышам референса (≈ 134 на свету), сохраняя читаемый контраст с цветным парапетом.
12. WHEN строятся стадион, лотки рынка и опоры эстакады ЛРТ, the system SHALL запекать AO контакта с землёй так же, как у корпусов домов (затемнение основания и ореол цвета покрытия).

- **AC-19.11** Given `cloudVisibility(h)`, When `h ≤ CLOUD.FADE.HIDE`, Then 0; When `h ≥ CLOUD.FADE.SHOW`, Then 1; между — монотонно (unit); and при `CLOUD.FADE.HIDE` отношение `h / (h − CLOUD.ALTITUDE)` ≤ 2,5. Given затухание 0 < v < 1 или v = 0, When кадр отрисован, Then видимые облака не отбрасывают тень, а тень даёт невидимый двойник с общей матрицей инстансов; при v = 1 тень даёт само облако, двойник выключен (unit на флаги).
- **AC-19.12** Given оракул Ламберта, When считается кровля на свету, Then яркость ∈ [110, 140]; and каждый цвет парапета отличается от кровли на ≥ 60 в sRGB (расстояние по каналам; уточнено при реализации: по одной яркости красный и синий бортики близки к серому); and зимой кровля под снегом светлее летней (unit).
- **AC-19.13** Given квартал `stadium`, `market` и чанк с эстакадой ЛРТ, When собраны, Then у чаши стадиона, у каждого лотка и у каждой опоры есть ореол AO цвета покрытия под ними; потолки 7 000 / 9 000 соблюдены (unit).

**Волна 3 (2026-09-23, стадион):**

13. WHEN строится стадион, the system SHALL делать чашу открытой сверху, как у референса: наклонные ярусы трибун кольцами вокруг поля, внешняя стена чаши, открытое кольцо кровли по краю (раздвижная крыша «Астана Арены») и поле с полосами газона и разметкой — вместо сплошного диска кровли, который закрывал поле и трибуны.

- **AC-19.14** Given стадион, When собран, Then над центральной частью поля нет геометрии выше поля, кроме мачт (луч сверху в поле попадает в газон или разметку); ярусы трибун — кольца, обращённые к полю (нормаль с компонентами вверх и внутрь); разметка поля ≥ 10 элементов; квартал `stadium` ≤ 7 000 вершин (unit).

**Волна 4 (2026-09-23, ландмарки):**

14. WHEN строится ландмарк, the system SHALL запекать AO контакта с землёй у его главных объёмов (подиум, корпус, основание): затемнение низа стен и ореол цвета той плиты, на которой объём стоит (с её высотой и границей). Исключение — Байтерек: вокруг его основания лежат концентрические кольца разных цветов, одноцветный ореол читался бы чужим.

- **AC-19.15** Given каждый из 11 ландмарков (кроме Байтерека), When собран квартал, Then в статике есть ореол: вершины на высоте плиты + `AO.GROUND_LIFT` с цветом плиты × `AO.GROUND_MIN` у объёма и ровно цвет плиты у внешнего края, не выходящие за границу плиты; потолок 9 000 вершин соблюдён; снапшоты генератора без изменений (unit).

**Волна 5 (2026-09-24, сравнение кадров после волны 4):**

15. WHEN строится стеклянная башня делового центра, the system SHALL рисовать на видимых фасадах сетку навесной стены — вертикальные импосты с шагом `FACADE.MULLION_STEP` поверх горизонтальных поясов, — чтобы фасад читался окнами, как у референса, а не гладкой синей коробкой.
16. WHEN строится Байтерек, the system SHALL запекать AO контакта у основания по разноцветным кольцам площадки: единый радиальный градиент затемнения от `AO.GROUND_MIN` у основания до 1 на ширине `AO.GROUND_WIDTH`, в каждом кольце — его собственным цветом.

- **AC-19.16** Given стеклянная башня 16 × 16 в 10 этажей, When собрана, Then на каждой из двух видимых сторон ≥ 4 вертикальных импоста во всю высоту, на скрытых сторонах — ни одного; квартал `business-glass` ≤ 7 000 вершин (unit).
- **AC-19.17** Given Байтерек, When собран, Then у основания лежат полосы затемнения каждого кольца в его цвете; множитель непрерывен на границах колец (± 1e-6) и растёт от `AO.GROUND_MIN` у основания до 1 на `r + AO.GROUND_WIDTH`; потолок 9 000 (unit).

**Волна 6 (2026-09-24, магазины):**

17. WHEN строится торговый ряд, the system SHALL делать маркизы наклонными (внешний край ниже стены) и полосатыми — белые полосы поперёк ширины поверх цвета маркизы, как у магазинов референса.
18. WHEN собирается коммерческий квартал, the system SHALL ставить кафе-террасу — настил и столики под зонтиками — перед фасадом заднего торгового ряда (к парковке или к улице — куда он смотрит при данном повороте квартала), не задевая маркизы, корпус и парковку; цвета зонтиков — из палитры по `variant`, без `rng`.

- **AC-19.18** Given торговый ряд, When собран, Then у каждой маркизы ≥ 2 белые полосы, внешний край маркизы ниже внутреннего, все маркизы — на видимой стороне (unit).
- **AC-19.19** Given коммерческий квартал при каждом из 4 поворотов, When собран, Then перед фасадом заднего ряда стоят ≥ 3 зонтика; ни один не залезает в корпус ряда, под маркизы и на асфальт парковки; квартал ≤ 7 000 вершин; снапшоты генератора без изменений (unit).

**Волна 7 (2026-09-24, облака):**

19. WHEN облако в кадре, the system SHALL рисовать его белым и мягким, как у референса: грани в тени — светло-серые с холодным оттенком, освещённые — почти белые, но не выжженные (грани различимы); геометрия облаков, их тени на земле и тон остального кадра не меняются.

- **AC-19.20** Given летний свет и материал облаков, When оракул Ламберта (`tests/render/tone.test.ts`) считает грани белого объёма облака, Then грань в тени (нормаль +Z) — в пределах ± 8 от замера референса (195, 200, 209) по каждому каналу, верхняя грань — от 238 до 252 по каждому каналу, освещённый бок (+X) — между ними; зимой грань в тени ≥ 185; сцена без облаков не меняется: `materials.opaque` не тронут (unit). Замер кадра с облаком — в qa-evidence.

**Волна 8 (2026-09-24, окна):**

20. WHEN строятся оконные проёмы домов (панельный, новостройка, торговый ряд, учебный корпус) и витрины торговых рядов, the system SHALL рисовать стекло тёмным, как у референса, — почти чёрным с холодным оттенком, а не насыщенно-синим; число вершин не меняется.

- **AC-19.21** Given летний свет, When оракул Ламберта считает стекло окна, Then на освещённой стороне (+X) каждый канал — в пределах ± 6 от замера ближних окон референса (37, 39, 48); в тени (+Z) стекло темнее, чем на свету, но не чёрная дыра — каждый канал ≥ 8; яркость стекла на свету ≤ 0,4 яркости любой стены жилых домов на свету летом и зимой — самая тёмная стена кирпич: 0,35 летом и 0,38 зимой, прежнее синее стекло на нём — 0,73 (unit). Все проёмы и витрины торговых рядов — цвета `window`; число вершин домов не меняется (unit).

**Волна 9 (2026-09-24, кровли ТЦ):**

21. WHEN строится торговый центр, the system SHALL заполнять его кровлю, как большие крыши референса: два ряда кондиционеров (светло-серый корпус с тёмным вентилятором сверху) и ряд световых фонарей-пирамид, у каждого — мягкое затемнение AO на кровле; раскладка постоянная, без новых вызовов `rng`; случайные детали крыши (`roofDetails`) — в своей полосе у фасада, не пересекаясь с установками.

- **AC-19.22** Given ТЦ 40 × 24 при обеих видимых сторонах (`hidden.z` ± 1), When собран, Then на кровле ≥ 8 кондиционеров с вентиляторами и ≥ 3 фонаря; все — внутри кровли и не пересекают друг друга и полосу случайных деталей; у каждой установки — ореол AO цвета кровли; `mall()` тратит `rng` ровно как один вызов `roofDetails` (следующее значение генератора совпадает); квартал ТЦ ≤ 7 000 вершин (unit).

**Волна 10 (2026-09-24, остальные большие кровли):**

22. WHEN строится торговый ряд или учебный корпус, the system SHALL ставить на кровлю ряд кондиционеров того же вида, что у ТЦ (FR-19.21): у торгового ряда — за вывеской, с шагом 8 по длине, у учебного корпуса — по два с каждой стороны от купола; у каждого — ореол AO кровли; без новых вызовов `rng`, случайные детали торгового ряда — в полосе перед рядом установок.

- **AC-19.23** Given торговый ряд 26 × 11 и 42 × 11 и учебный корпус 36 × 12 при обеих видимых сторонах, When собраны, Then у ряда ⌊(L − 8) / 8⌋ + 1 кондиционеров (3 и 5) — внутри кровли, за вывеской и мимо полосы случайных деталей; у корпуса — 4, не ближе 6,5 к оси купола; у каждого — ореол AO кровли; `shopRow()` тратит `rng` ровно как один `roofDetails`; кварталы торговый и учебный ≤ 7 000 вершин (unit).

**Волна 11 (2026-09-24, фасады новостроек):**

23. WHEN строится новостройка, the system SHALL делить её видимые фасады горизонтальными поясами-плитами на каждом междуэтажном уровне — светлыми, с выносом 0,5, как балконные пояса домов референса; на скрытых сторонах поясов нет, в видимом углу плиты двух сторон не перекрываются, с балконными плитами нет совпадающих граней.

- **AC-19.24** Given новостройка 12 × 12 в 10 этажей при каждом из 4 поворотов, When собрана, Then на уровнях `i · FLOOR` (i = 1…9) — по две плиты, всего 18; каждая вершина плит — снаружи корпуса на видимой стороне; объёмы плит попарно не пересекаются; верх плиты ниже верха балконной плиты на ≥ 0,01; квартал новостроек ≤ 7 000 вершин (перебор 4 сидов) (unit).

**Волна 12 (2026-09-24, вода):**

24. WHEN строится речной квартал, the system SHALL сажать воду в русло тем же языком AO, что и дома: стенки набережных темнеют к урезу воды, а на воде вдоль каждой стенки лежит полоса затемнения, растворяющаяся к середине русла.

- **AC-19.25** Given речной квартал, When собран, Then у обеих стенок набережной грань к воде у уреза — ≤ 0,55 цвета бетона, наверху — ≥ 0,85; на воде вдоль каждой стенки — полоса на всю длину воды: у стенки `water · AO.GROUND_MIN`, у южной стенки через `AO.GROUND_WIDTH` — `water`, у северной — за краем настила набережной ещё на `AO.GROUND_WIDTH`; квартал ≤ 7 000 вершин (unit).

**Волна 13 (2026-09-24, зелень деловой площади):**

25. WHEN собирается деловой квартал, the system SHALL озеленять мощёную площадь газонными островами с деревьями в её свободных частях — восточной, южной и западной, — не задевая башни с их ореолами AO, фонтан, скамейки, клумбы, велопарковку и парковку; швы мощения обходят острова; раскладка постоянная, без новых вызовов `rng`.

- **AC-19.26** Given деловой квартал из окна 21 × 21 seed `astana` при любом варианте, When собран, Then на площади — 3 газонных острова с ≥ 4 деревьями на них; острова не пересекают габариты башен с ореолом AO, фонтан, скамейки, клумбы, велопарковку и парковку; кроны деревьев островов — внутри плиты квартала; швы мощения не заходят на острова; квартал ≤ 7 000 вершин (unit). Доля зелени в стартовом кадре растёт (замер — в qa-evidence).

**Волна 14 (2026-09-24, зелень рынка):**

26. WHEN собирается рынок, the system SHALL сажать его боковые края на газонные полосы с деревьями — восточную (под уже стоящими деревьями) и западную (под кустами), не задевая павильон с ореолом AO, торговые ряды, столбики и клумбу; без новых вызовов `rng`.

- **AC-19.27** Given рынок из окна 21 × 21 seed `astana`, When собран, Then у него 2 газонные полосы, на них ≥ 4 дерева (два прежних и два новых); полосы не пересекают павильон с ореолом, лотки, столбики, клумбу; кроны новых деревьев не касаются соседних крон и лотков; квартал ≤ 7 000 вершин (unit).

**Волна 15 (2026-09-25, пешеходные переходы):**

27. WHEN собирается чанк с дорогами, the system SHALL рисовать пешеходные переходы как у референса — жирной «зеброй» из 8 полос поперёк всей проезжей части, от бордюра до бордюра, с каждой из четырёх сторон перекрёстка и вне его зоны; стоп-линия лежит на подъезжающей полосе перед зеброй с зазором; осевые и краевые линии и стрелки не касаются зебр и стоп-линий; машина, которой нужно уступить на перекрёстке, останавливается передом перед стоп-линией, а не на зебре.

- **AC-19.28** Given чанк с дорогами — обычный, речной и под ЛРТ, When собран, Then в нём 4 зебры по 8 полос: у своего перекрёстка с восточной и южной сторон и у дальних концов своих дорог — с западной и северной сторон перекрёстков соседей; полосы зебр — на асфальте вне зоны перекрёстка `[−30; −20]²` и внутри чанка и перекрывают проезжую часть поперёк не меньше чем на 90 %; перед каждой зеброй на подъезжающей полосе — стоп-линия с зазором ≥ 0,5; осевые и краевые линии и стрелки не касаются зебр и стоп-линий; разметка чанка не дороже прежней по вершинам (unit). Given машина, которой нужно уступить на перекрёстке, When она подъезжает, Then останавливается передом до стоп-линии; машина, уже проехавшая стоп-линию, как и раньше, встаёт у края зоны (unit); 2 и 3 минуты симуляции — 0 пересечений и 0 заторов (unit), e2e симуляции зелёная.

**Волна 16 (2026-09-25, карточка ссылки и значок):**

28. WHEN ссылку на демо пересылают в мессенджер или соцсеть, the system SHALL отдавать карточку, как у референса: заголовок, описание и картинку 1200 × 630 со стартовым кадром города (Open Graph и Twitter Card, абсолютные `https`-адреса). WHEN страница открыта во вкладке, the system SHALL показывать значок — силуэт Байтерека в цветах палитры: SVG для современных браузеров и PNG — для Safari до 26 и домашнего экрана iOS. Цвет интерфейса мобильного браузера (`theme-color`) — небо текущей палитры. Экранных кнопок не добавляется (решение пользователя, итерация 2).

- **AC-19.29** Given `index.html`, When его читает парсер, Then в нём есть `og:type`, `og:site_name`, `og:locale`, `og:title`, `og:description`, `og:url`, `og:image` — абсолютный `https`-адрес `og.jpg` сайта, `og:image:width` 1200, `og:image:height` 630, `og:image:alt`, `twitter:card` = `summary_large_image`, и ссылки на `favicon.svg`, `favicon-32.png` и `apple-touch-icon.png`; эти файлы лежат в `public/`: `og.jpg` — JPEG 1200 × 630, значки — PNG 32 × 32 и 180 × 180; все цвета SVG-значка — цвета палитры (unit). Given собранное демо, When оно открыто, Then `theme-color` страницы равен цвету неба палитры, а файлы значков отдаются с кодом 200 (e2e).

**Волна 17 (2026-09-25, дворы):**

29. WHEN строится жилой квартал, the system SHALL ставить во двор настоящую детскую площадку, как во дворах Астаны: игровой домик с крышей и горкой, качели на раме, карусель — на песчаной площадке 7 × 7, в ярких цветах палитры; у панельных домов она заменяет прежнюю из трёх коробок, во дворе новостройки — появляется на новом газоне у юго-западного края, где двор был пустым мощением. Без новых вызовов `rng`: случайные деревья обходят площадку.

- **AC-19.30** Given `Props.playground`, When собрана, Then в ней песчаная площадка 7 × 7, домик с крышей, горка от домика до земли, качели (рама и 2 сиденья), карусель; вся геометрия — внутри площадки и не выше 2,6; ≤ 350 вершин; горка съезжает к видимой стороне квартала (`facing`, D13), при −1 площадка зеркальна по Z (unit). Given новостройка из окна 21 × 21 seed `astana` при любом варианте, When собрана, Then во дворе — газон и на нём площадка, которая не пересекает башни с ореолами AO, клумбу, фонари и парковку; ни одно дерево не стоит на площадке; квартал ≤ 7 000 вершин; панельный квартал — тоже ≤ 7 000 (unit).

---

### FR-20: Зелёный старт — сквер между Байтереком и Хан Шатыром (решение пользователя 2026-09-24 «зелёный старт»)
**User Story:** Как зритель, я хочу, чтобы первый кадр был таким же зелёным, как у Infinitown, чтобы город с первого взгляда читался живым, а не мощёным.

**Контекст (замер 2026-09-24).** Доля «зелёных» пикселей стартового кадра (g выше r и b на 25+) по 12 seed: средняя 10,4 %, медиана 11,5 %, худший 4,3 %; у референса — 15,7 %. Стартовая зона при любом seed лежит на левом берегу, где веса типов — деловые 6, ТЦ 4,5, новостройки 3, а парки 0,7 (≈ 4,5 % кварталов). Центр кадра — чанк (0, 0) между стартовыми Байтереком (0, −1) и Хан Шатыром (0, 1); в Астане между ними — бульвар Нуржол с газонами и фонтанами.

**Requirements (EARS):**
1. WHEN вычисляется тип квартала чанка между стартовыми ландмарками (0, 0), the system SHALL делать его парком при любом seed.
2. WHEN центр старта — парк, the system SHALL сохранять остальные правила генерации: соседи центра не повторяют его тип (FR-3.2), стартовые ландмарки на местах (FR-4.1), вне центра — прежние веса берегов.
3. WHEN правила генерации меняются, the system SHALL поднимать версию генератора (`GEN.VERSION` 3 → 4): ссылки со старой версией открывают обновлённый город с уведомлением «Город обновился».
4. WHEN строится парк центра старта, the system SHALL строить его бульваром Нуржол (решение пользователя 2026-09-24 «сделай бульвар Нуржол»): мощёная аллея вдоль оси Байтерек — Хан Шатыр на всю длину квартала, фонтаны на оси с водными каналами между ними, ряды деревьев вдоль аллеи, газоны с клумбами по бокам, скамейки и фонари; при любом повороте квартала ось смотрит на Байтерек и Хан Шатыр; без `rng`, генератор не меняется.

**Priority:** Must · **Complexity:** Low · **Dependencies:** FR-2, FR-3, FR-4

**Acceptance Criteria:**
- **AC-20.1** Given 1 000 seed, When генерируется стартовая зона, Then чанк (0, 0) — парк без ландмарка, Байтерек и Хан Шатыр на своих клетках, ни один из 8 соседей центра — не парк; `GEN.VERSION` = 4 (unit).
- **AC-20.3** Given парк центра старта при каждом из 4 поворотов, When собран, Then его мощёные дорожки тянутся на всю длину квартала вдоль мировой оси Байтерек — Хан Шатыр по обе стороны зелёной срединной полосы; на оси ≥ 3 фонтана, между соседними — водный канал; по ≥ 6 деревьев в ряду с каждой стороны, кроны не касаются друг друга, нависают над дорожками, но не над срединной полосой с фонтанами, фонари — не под кронами; швы мощения только на дорожках и вне площади у центрального фонтана; обычный парк вне центра — прежняя раскладка; квартал ≤ 7 000 вершин (unit).
- **AC-20.2** Given стартовый кадр 12 seed (`astana`, `almaty`, `steppe`, `esil`, `baiterek`, `nurzhol`, `ishim`, `saryarka`, `akmola`, `tselina`, `expo`, `zhibek`), When считаем долю «зелёных» пикселей, Then средняя ≥ 14,5 %, худший ≥ 9 % (замер скриптом, evidence — в qa-evidence).

---

## Non-Functional Requirements

### NFR-1: Производительность
- WHEN демо работает на десктопе среднего уровня (интегрированная графика уровня Intel Iris Xe, 1080p), the system SHALL держать медианный FPS ≥ 50 и ≥ 30 в 1 % худших кадров.
- WHEN демо работает на телефоне среднего уровня (Android 2022–2024, iPhone 12+), the system SHALL держать медианный FPS ≥ 30 при сниженных настройках (тени 1024, pixel ratio ≤ 1.5, меньше машин).
- WHEN отрисован кадр, the system SHALL укладываться в ≤ 300 draw calls (включая проход теней) и ≤ 400 000 треугольников.
- WHEN страница открыта на соединении 10 Мбит/с, the system SHALL показать первый кадр города ≤ 8 с; суммарный вес ассетов первого экрана ≤ 8 МБ (gzip/KTX2), JS-бандл ≤ 700 КБ gzip.
- WHEN окно чанков полностью обновляется (переход на новый чанк), the system SHALL тратить ≤ 8 мс на сборку новых чанков в кадре (сборка распределяется по кадрам или берётся из кэша).

### NFR-2: Совместимость
- WHERE браузер — последние 2 версии Chrome, Edge, Firefox, Safari (desktop и iOS/Android), the system SHALL работать на WebGL 2; WebGPU — опциональный флаг, не обязателен.
- WHEN устройство с touch, the system SHALL не вызывать прокрутку/зум страницы во время жестов на канвасе.

### NFR-3: Детерминизм
- WHEN генератор вызывается с одинаковыми (seed, gx, gy), the system SHALL возвращать идентичный дескриптор на любой платформе: только целочисленные хеши и собственный PRNG, никакого `Math.random` в генерации.
- WHEN симуляция машин/поездов/облаков выполняется, the system SHALL использовать реальное время (dt), а не «на кадр» — визуальная скорость не зависит от FPS.

### NFR-4: Качество кода и тестируемость
- TypeScript strict, ESLint + Prettier, unit-покрытие генератора/математики/трафика ≥ 80 %, e2e smoke на Playwright, visual regression при фиксированном seed.
- Вся конфигурация (размеры, вероятности, скорости, палитра) — в одном `config.ts`/`palette.json`, без «магических чисел» в логике.

### NFR-5: Лицензии и атрибуция
- Только CC0/собственные ассеты; список источников — в About и `CREDITS.md`. Код и ассеты Infinitown не копируются.

### NFR-6: Доступность
- Управление доступно с клавиатуры (панорамирование, открыть/закрыть About по `?`/Esc); при `prefers-reduced-motion` анимация заголовка заменяется мгновенным показом; контраст текста UI ≥ 4.5:1.

### NFR-7: Надёжность
- Ошибки генерации одного чанка не должны ронять приложение: чанк заменяется «парковым» fallback-чанком, ошибка логируется (в debug — на оверлей).

---

## Constraints and Assumptions

### Technical Constraints
- Статический хостинг (GitHub Pages / любой CDN), без серверной части.
- Пайплайн ассетов без Unity: glTF из CC0-наборов + процедурная геометрия ландмарков в коде (при необходимости — Blender → glTF).
- Один язык UI на старте — русский (тексты вынесены в словарь для будущей локализации).

### Business Constraints
- Один разработчик; спека рассчитана на поэтапную сдачу: Must → Should → Could.

### Assumptions
- Зритель смотрит демо 1–5 минут; главное — первый экран и плавность.
- Стартовый вид важнее «дальних» окраин: ландмарки Must гарантированно в стартовом окне.
- Реальная планировка Астаны не воспроизводится; «астанинскость» достигается ландмарками, ЛРТ, палитрой и типами застройки.

---

## Success Criteria

### Definition of Done
- [ ] Все Must-требования (FR-1…FR-6, FR-8…FR-11) выполнены и подтверждены AC.
- [ ] NFR-1…NFR-7 подтверждены измерениями (отчёт QA в `tasks.md`/QA-evidence).
- [ ] Should-требования (FR-7, FR-12, ландмарки Should) выполнены или явно перенесены.
- [ ] Visual-regression эталоны для seed `astana` зафиксированы.

### Acceptance Metrics
- Стартовый кадр: Байтерек + Хан Шатыр + ЛРТ-коридор видны (100 seed).
- Медианный FPS ≥ 50 (desktop), ≥ 30 (mobile); draw calls ≤ 300.
- Время до первого кадра ≤ 8 с на 10 Мбит/с.
- 0 пересечений машин и 0 «замёрзших» машин за 5 минут симуляции.

## Open Questions — закрыты допущениями (2026-09-17, пользователь: «Работаем»)
Принятые допущения (меняются только по явному указанию пользователя):
1. Should-ландмарки в порядке приоритета: Нур Алем → Пирамида → Ак Орда; Could: Астана Опера, Хазрет Султан, Абу-Даби Плаза.
2. Название демо: «Astana Infinite» (в UI кириллицей — «Астана без края» как подзаголовок About).
3. Хостинг: GitHub Pages.
4. UI на старте только русский; словарь строк готов к локализации.
5. Городские автобусы — сине-голубые (цвета флага), такси — жёлтые. **Пересмотрено 2026-09-17 (FR-16):** автобусы — брендинг CTS 2024 (светлый кузов, сине-зелёно-жёлтые элементы), такси — классы Яндекс Go.

## Open Questions (исходные, для истории)
1. Какие Should/Could-ландмарки приоритетнее: Нур Алем, Пирамида, Ак Орда, Астана Опера, Хазрет Султан, Абу-Даби Плаза?
2. Название демо для заголовка (рабочее: «Astana Infinite» / «Астана без края»)?
3. Хостинг: GitHub Pages достаточно?
4. Нужна ли англ. версия UI на старте?
5. Стиль машин/автобусов: реальные цвета городских автобусов Астаны (сине-голубые) — ок?

## Glossary

| Термин | Определение |
|---|---|
| Чанк | Квадратная ячейка мира 60×60 юнитов: квартал + дороги вдоль двух сторон + перекрёсток |
| Квартал (block) | Застроенная часть чанка (один из типов каталога) |
| Окно (window) | Видимая сетка слотов W×W (по умолчанию 9×9), в которые подставляются чанки |
| Слот | Object3D в фиксированной позиции окна, содержимое подменяется при сдвиге |
| gridCoords | Целочисленные координаты чанка мира, находящегося в центральном слоте |
| Seed | Строка, полностью определяющая город |
| Ландмарк | Уникальный объект-доминанта (Байтерек, Хан Шатыр…), занимающий квартал чанка |
| ЛРТ-коридор | Ряд чанков, вдоль дороги которого идёт эстакада ЛРТ |
| Моб (mobile object) | Движущийся объект, живущий внутри чанка и переносимый между чанками (машина, поезд, облако) |
| Радар | Область перед машиной, в которой она ищет другие машины для торможения |
